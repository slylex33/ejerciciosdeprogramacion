function calculaarea(base, altura) {
  var area = (base * altura) / 2;
  return area;
}
var basedeltriangulo = 30;
var alturadeltriangulo = 40;
var areadeltriangulo = calculaarea(basedeltriangulo, alturadeltriangulo);
console.log("El area es: " + areadeltriangulo);
